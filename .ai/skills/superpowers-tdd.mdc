# 测试驱动开发（TDD）

> 刚性技能：实现任何新功能或修复 Bug 时**必须**遵循。
> "如果不先看测试失败，你就不知道它测试的是否正确。"

## 铁律

**没有失败的测试，就不能编写生产代码。**

如果你先写了代码，必须将其**删除**并重新开始。不能保留作为"参考"。

## 适用范围

- 新功能
- Bug 修复
- 重构（行为变更时）

### 例外（需用户同意）

- 丢弃型原型
- 生成的代码
- 配置文件

## Red-Green-Refactor 循环

### RED：编写失败的测试

1. 编写一个**最小化**的测试来展示应有的行为
2. 测试名称要清晰
3. 测试真实逻辑（避免使用 Mock，除非不可避免）
4. **运行测试并看着它失败**
5. 如果测试通过 → 说明你在测试现有行为，修改测试

### GREEN：编写最小代码

1. 编写最简单的代码以使测试通过
2. **禁止**：
   - 不要添加额外功能
   - 不要重构其他代码
   - 不要"改进"超出测试要求的范围（YAGNI）

### REFACTOR：清理

1. **仅在所有测试通过后**进行
2. 去除重复
3. 改进命名
4. 提取辅助函数

## 质量检查清单

标记工作完成前，确认：

- [ ] 每个新函数都有测试
- [ ] 在实现之前看着每个测试失败
- [ ] 每个测试都因预期的原因失败（功能缺失，而非拼写错误）
- [ ] 编写了最小代码以通过测试
- [ ] 所有测试通过，且输出干净

## 红旗警告：停下来，从头开始

- 在测试之前写了代码
- 测试立即通过
- 无法解释测试为何失败
- 找理由说"就这一次"
- 试图保留现有代码作为参考

## 常见借口与反驳

| 借口 | 事实 |
|------|------|
| "之后再写测试是一样的" | 后写的测试只验证代码"做了什么"，而非"该做什么"。容易遗漏边界情况 |
| "我已经手动测试了" | 手动测试无记录，无法在代码变更后复现 |
| "TDD 太教条" | TDD 才是真正务实——提交前发现 bug 比之后调试快 |
| "删掉 X 小时的工作太浪费了" | 沉没成本谬误。保留未经测试验证的代码是技术债务 |
