# 验证优先于完成（Verification Before Completion）

> 刚性技能：在声称任何工作"完成"之前**必须**执行。
> "证据先于断言。"

## 铁律

**没有最新的验证证据，不得做出完成声明。**

如果在当前消息中没有运行验证命令，就不能声称它通过了。

## 门控流程

在进行任何状态声明之前：

1. **识别**：什么命令能证明这一声明？
2. **运行**：执行**完整**的命令（全新、完整）
3. **阅读**：阅读完整输出，检查退出代码，统计失败次数
4. **验证**：输出是否确认了声明？
   - 否 → 带着证据说明**实际状态**
   - 是 → 带着证据说明声明

## 验证要求表

| 声明 | 必需条件 | 不充分的条件 |
|------|---------|------------|
| 测试通过 | 测试命令输出：0 次失败 | 之前的运行结果、"应该能过" |
| Linter 干净 | Linter 输出：0 错误 | 部分检查、推断 |
| 构建成功 | 构建命令：退出码 0 | Linter 通过、"日志看起来没问题" |
| Bug 已修复 | 测试原始症状：通过 | 代码已更改、"假设已修复" |
| 回归测试有效 | 已验证 Red-Green 循环 | 测试通过一次 |
| 满足需求 | 逐行检查清单 | "测试通过了" |
| 文档已同步 | 总览文档与实际代码/设计一致 | "之前同步过了"、"没什么变化" |

## 关键模式

### 测试
✅ 运行测试 → 查看结果 → 声明"所有测试通过"
❌ "现在应该能通过了" / "看起来是对的"

### 回归测试（TDD Red-Green）
✅ 编写 → 运行（通过）→ 撤销修复 → 运行（必须失败）→ 恢复修复 → 运行（通过）
❌ 仅说"我写了回归测试"

### 构建
✅ 运行构建 → 查看退出码 0 → 声明"构建通过"
❌ "Linter 通过了"（Linter 不检查编译）

### 需求满足
✅ 重读计划 → 创建清单 → 逐项验证 → 报告缺口或完成
❌ "测试通过，阶段完成"

## 红旗警告：停止

- 使用"应该"、"大概"、"似乎"等词汇
- 在验证之前表达满意（"太好了！"、"完美！"、"做完了！"）
- 即将提交代码却未进行验证
- 依赖部分验证
- 心想"就这一次"
- 因疲惫而想结束工作

## 底线

**验证没有捷径。**
运行命令。阅读输出。**然后**再声明结果。
这是不可协商的。
