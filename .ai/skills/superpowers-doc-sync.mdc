# 文档同步（Document Synchronization）

> 刚性技能：每次迭代完成后**必须**执行。
> "总览文档是项目的唯一真相源。"

## 铁律

**每次功能迭代完成后，必须将迭代产出同步回总览文档。总览文档过时 = 项目失忆。**

## 文档体系

### 总览文档（Single Source of Truth）

位于 `design/` 目录，始终反映项目的**最新全貌**：

| 文档 | 职责 | 路径 |
|------|------|------|
| 需求文档 | 完整的功能需求、交互需求、阶段规划 | `design/requirements.md` |
| 架构设计 | 技术栈、AI 模型策略、数据存储、Provider 接口 | `design/architecture.md` |
| 项目结构 | 运行时目录结构、关键文件说明 | `design/project-structure.md` |
| 技术风险 | 风险清单、用户反馈、调研结论、风险矩阵 | `design/risks.md` |

### 迭代文档（Per-Feature）

每次功能开发产出的文档，记录**特定功能**的设计与实施细节：

| 文档类型 | 路径 | 产出时机 |
|---------|------|---------|
| 功能设计 | `designs/YYYY-MM-DD--<feature-name>.md` | 头脑风暴阶段 |
| 实施计划 | `docs/plans/YYYY-MM-DD-<feature-name>.md` | 编写计划阶段 |

### 两者关系

```
迭代文档（局部、详细、时间点快照）
    ↓ 同步回流
总览文档（全局、概要、始终最新）
```

迭代文档**不删除**，作为决策历史存档。总览文档始终是**当前最新状态**的权威来源。

## 同步触发时机

以下事件发生后，**必须**检查并更新总览文档：

1. **功能开发完成**（通过验证规则后）
2. **风险状态变化**（新风险发现、风险缓解、风险关闭）
3. **架构决策变更**（技术栈更换、接口重构、存储方案变化）
4. **需求变更**（新增/删除/修改功能需求）
5. **项目结构变化**（新增目录/文件、重组结构）

## 同步流程

### Step 1: 差异识别

对比本次迭代的变更，逐一检查总览文档是否需要更新：

- [ ] `design/requirements.md` — 是否有新增/修改的功能需求？阶段规划是否需要更新？
- [ ] `design/architecture.md` — 是否引入了新技术/库？接口设计是否有变化？AI 模型策略是否调整？
- [ ] `design/project-structure.md` — 是否新增了目录/文件？数据结构是否有变化？
- [ ] `design/risks.md` — 是否有新风险？已有风险是否已缓解/关闭？

### Step 2: 增量更新

对需要更新的总览文档进行**增量修改**（不是重写）：

- **新增内容**：在对应章节追加，保持已有内容不变
- **修改内容**：精确替换变化的部分
- **删除内容**：标记为已废弃或删除，保持文档结构完整
- **阶段标记**：已完成的 Phase 项打勾 `[x]`

### Step 3: 一致性校验

更新后检查总览文档之间的一致性：

- [ ] `requirements.md` 中提到的模块，在 `architecture.md` 中有对应的技术方案
- [ ] `architecture.md` 中的技术选型，在 `risks.md` 中有对应的风险评估
- [ ] `project-structure.md` 中的目录结构，与实际代码仓库一致
- [ ] `risks.md` 中的风险矩阵总览表与各风险项状态一致

### Step 4: 提交

- 总览文档的更新与功能代码**分开提交**或在同一次提交中明确标注
- 提交信息格式：`docs: sync overview docs after <feature-name>`

## 开发前必读

在开始任何新功能的头脑风暴之前，**必须先阅读**以下总览文档以获取最新上下文：

1. `design/requirements.md` — 理解功能全景和当前阶段
2. `design/architecture.md` — 理解技术栈和设计约束
3. `design/project-structure.md` — 理解数据结构和目录布局
4. `design/risks.md` — 理解已知风险和缓解方案

**不读总览就开始设计 = 可能重复造轮子或与已有架构冲突。**

## 与其他技能的关系

```
头脑风暴 ──→ 风险评估 ──→ 编写计划 ──→ TDD ──→ 验证 ──→ 审查
    ↑                                                        ↓
    │                                                   文档同步（本技能）
    │                                                        ↓
    └──────────── 读取最新总览 ←──────────── 更新总览文档
```

## 危险信号

当出现以下情况时，**停止并执行本流程**：

- 总览文档的描述与实际代码不一致
- 新功能设计依赖的信息在总览文档中找不到
- 两个迭代之间没有同步过总览文档
- 无法确定某个设计决策是否仍然有效
- 开始头脑风暴时对"现在项目是什么状态"感到模糊
