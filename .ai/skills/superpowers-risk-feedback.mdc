# 风险提取与反馈（Risk Extraction & Feedback Loop）

> 刚性技能：在头脑风暴产出设计后、编写实施计划前**必须**执行。
> "未经风险评估的计划是赌博。"

## 铁律

**设计方案未经风险提取、用户反馈、调研验证三步闭环，不得进入计划编写阶段。**

## 触发时机

头脑风暴设计被用户批准后，立即进入本流程。

## 三步闭环

### 第一步：风险提取

从设计方案中**主动**识别技术风险和难点。每项风险必须包含以下维度：

| 维度 | 说明 |
|------|------|
| **问题** | 一句话描述核心问题 |
| **风险等级** | 高 / 中 / 低 |
| **影响** | 影响哪些模块、阻塞什么 |
| **难点** | 具体的技术难点 |
| **风险** | 可能出现的最坏情况 |
| **缓解** | 初步缓解方案或备选路径 |

#### 风险识别清单

必须检查以下维度：

- [ ] **外部依赖**：第三方 API/SDK 是否成熟？文档是否完善？是否有替代方案？
- [ ] **技术可行性**：核心技术路径是否有验证过的先例？
- [ ] **性能**：在目标数据规模下能否满足性能要求？
- [ ] **跨平台**：macOS / Windows / Linux 上行为是否一致？
- [ ] **数据格式**：输入输出格式是否有兼容性问题？
- [ ] **AI 模型**：模型精度是否满足需求？本地推理性能如何？
- [ ] **集成点**：与已有模块/外部工具的集成是否存在断裂？

### 第二步：用户反馈

将风险列表呈现给用户，**逐项**收集反馈。

用户反馈的典型形式：
- **方向性意见**："是否能走 XXX 路径"、"固定做某些操作"
- **约束补充**："这个应该不是问题"、"我可以提供 XXX"
- **优先级调整**："先实际尝试吧"、"这个依赖于另一个风险"
- **资源信息**："我用的是 XXX 设备/服务"、"我有 XXX 条件"

**记录方式**：在每项风险的表格中追加 `反馈` 行，原文记录用户意见。

### 第三步：调研验证

根据用户反馈，**逐项**进行深度调研。调研必须：

1. **验证可行性**：明确标注 ✅ 可行 / ❌ 不可行 / ⚠️ 有条件可行
2. **给出具体方案**：不是"应该可以"，而是具体的技术路径、库/工具名称、版本要求
3. **标注前提条件**：方案生效的前提（如需要 Studio 版、需要特定硬件等）
4. **保留 Fallback**：主方案之外的降级方案

**记录方式**：在每项风险的表格中追加 `调研` 行，记录调研结论。

## 文档格式

风险文档保存到 `design/risks.md`（如已存在则更新），格式：

```markdown
## RX. [风险名称]

**风险等级**：高/中/低

| 维度 | 说明 |
|------|------|
| 问题 | ... |
| 影响 | ... |
| 难点 | ... |
| 风险 | ... |
| 缓解 | ... |
| 反馈 | [用户原话] |
| 调研 | [调研结论，含可行性标注] |
```

文档末尾维护**风险矩阵总览**表格：

```markdown
| ID | 风险项 | 等级 | 阶段影响 | 缓解难度 |
|----|--------|------|----------|----------|
```

## 闭环判定

完成以下条件后，方可进入"编写计划"阶段：

- [ ] 所有**高风险**项已完成调研，且有明确的缓解方案或 Fallback
- [ ] 所有**中风险**项已完成调研，或用户明确表示"实际尝试"
- [ ] 用户对风险评估结果**已确认**
- [ ] 风险矩阵已更新到 `design/risks.md`

## 与其他技能的关系

```
头脑风暴 → [设计批准] → 风险提取与反馈 → [风险评估通过] → 编写计划 → TDD → 验证 → 审查
                              ↑                                              |
                              └──── 实施中发现新风险时，回到此流程 ────────────┘
```

### 实施阶段的风险回流

在 TDD / 调试 / 验证阶段发现**设计文档未预见的风险**时：

1. 停止当前实施
2. 将新风险追加到 `design/risks.md`
3. 告知用户，收集反馈
4. 调研验证
5. 必要时更新实施计划

## 危险信号

当出现以下想法时，**停止并执行本流程**：

- "这个方案肯定行"（未调研验证）
- "先做了再说"（跳过风险评估）
- "风险不大"（凭直觉而非证据）
- "文档里已经有了"（未确认是否还适用于当前设计）
