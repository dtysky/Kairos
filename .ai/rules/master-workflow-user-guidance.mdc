---
description: 当用户询问如何使用 Kairos 或发起全流程任务时，必须按统一工作流口径引导
alwaysApply: true
---

# Master Workflow User Guidance

当用户在 Kairos 工程里提出以下类型的问题时，适用本规则：

- “这个系统怎么用”
- “帮我从头做一个项目”
- “继续这个项目，下一步该做什么”
- “把整个流程串起来”
- 未明确指明 phase，只是笼统要求开始做片子、继续做片子、导出成片

## 总原则

- 必须把 Kairos 解释成一个 **准备阶段 + 主链阶段 + Edit Flow 原子能力流** 的工作流，而不是一组互不相关的脚本。
- 必须优先遵循 `kairos-workflow` skill 作为总控口径，再根据当前阶段调用对应的 phase skill。
- 当用户没有明确说明当前已经做到哪一步时，先根据项目内已有产物判断可恢复阶段，再继续，不要默认从头重跑。
- 对用户的说明应优先使用“下一步做什么 + 为什么 + 需要用户提供什么”的方式，而不是只罗列内部实现细节。
- 如果当前任务涉及需求、行为、工作流、正式入口或用户路径变更，必须额外遵守 `change-management-discipline.mdc`：先进入 `Plan` 模式（或宿主等价的结构化计划确认），再更新设计文档、再实现，最后回查并同步设计文档 / rules / skills。
- 当前正式控制面是 `Supervisor + React console (apps/kairos-console/)`；`/analyze` 与 `/style` 是正式监控路由，旧静态监控页和兼容脚本不能再被解释成新的正式入口。
- `scripts/kairos-supervisor.* start` 只会启动 `Supervisor + React console`，不会顺带启动 ML，也不会恢复旧 job。
- `progress.json` 只能被解释成 durable cache；“是否还在跑”必须看 live job record。
- ML-backed Kairos 官方流程结束态必须是 `ML stopped`；`spatial-refresh / chronology-build` 是 `/chronology` 的 no-ML deterministic job，`span-rebuild` 会进入 ML lifecycle 调用本地 qwen 文本 LM，但不重跑 VLM / ASR。

## 对用户的标准流程口径

Kairos 的标准顺序是：

1. `Edit Rules` + 可选的 `Style Analysis`
2. `Project Init`
3. `Ingest`
4. `Analyze`
5. `Chronology Review`
6. `Edit Flow`
7. `Export`

向用户解释“怎么用”时，默认按下面的口径组织：

- 第一步先确认项目是否已经初始化；如果没有，先建立 `projects/<projectId>/`
- 第二步让用户维护 `config/project-brief.md`，把素材根目录和说明写进去
- 第三步执行 Ingest，把素材扫描进 `store/assets.json`
- 第四步执行 Analyze，只生成 `analysis/asset-reports/*.json` 作为分析事实真相
- 素材主键协议是 `materialIdPolicyVersion=human-source-v1`：root 必须有稳定 `rootCode`，新资产和 span 不得再使用随机 UUID，也不得使用 `asset__` / `span__` namespace 前缀；审查、report、chronology、material-slots 和 timeline 都以短 source locator 作为正式外键
- 第五步进入 `/chronology`：先显式生成 fresh `store/spans.json`，再生成、审查并确认 Chronology V2；素材时间真值是已修正的 `asset.capturedAt`，Chronology 的 `sortCapturedAt` 只是兼容镜像；Edit Flow 先以 `schemaVersion=2.0` 且 `status=confirmed` 的 chronology 为入口，后续 step 只按 confirmed Flow Plan 的 `inputRefs` 要求消费 spans / assets / asset reports
- 第六步在 `/edit` 进入 Edit Flow：
  - 初始化 Edit Unit：选择/创建 `editId`，选择 workspace `editRuleCategory`，可选选择 `styleCategory`，写入 `edits/<editId>/config/edit-unit.json`
  - Flow Plan、剪辑框架、素材召回等产物由 Codex Agent 直接在仓库内生成和维护；`/edit` 只读展示，不提供生成 Flow、确认 Flow、运行 step、运行下一步或确认 gate 的按钮
  - Flow Plan 使用 `plannerPolicyVersion=codex-agent-v1`，只选择剪辑规则明确要求或后续执行必需的最小 capability 集合；不要把 capability registry 当成固定阶段链
  - `trip.event_table` / `material.archive` 只在剪辑规则明确要求独立事件表、行程表、素材档案、素材库文档或单独人工审查产物时出现
  - `edit.framework` 只做剪辑 handoff：`全片章节` 是宏观概览，`分段操作稿` 是唯一可执行 FW beat 边界，不输出 `beat 边界索引`；主表和 `给 material.recall 的执行索引` 都不得写 chronology/event/route/gap/span/asset id；`spans` 列必须写类型数量与视频语音拆分，`叙事` 只写客观素材总结；召回索引只写全局召回、排序、静音、加速规则
  - Codex Agent 按 confirmed Flow Plan 的 capability steps 逐步运行和写产物；人工审查通过产物本身完成，不通过 Console runner
  - 中间产物由每个 capability 的 `outputRefs` 决定；不要要求固定 `script/current.json`、固定 beat 或固定 `Script -> Timeline` 顺序
  - `resolve.media_sync` 只在 Flow Plan 声明时同步达芬奇 Media Pool；`timeline.generate` 只从已同步 Media Pool 选素材，不重新导入或清空 namespace
- 第七步在目标 NLE 和导出目录明确后执行 Export

## 用户真正该编辑的入口

默认应把以下文件视为用户入口，而不是要求用户直接改中间产物：

- `config/project-brief.md`
- `config/manual-itinerary.md`
- `<workspaceRoot>/config/edit-rules/{category}.md` 或用户明确指定的剪辑规则
- `<workspaceRoot>/config/styles/{category}.md` 或用户明确指定的文案 / 艺术风格参考
- `/edit` 中确认的 `edits/<editId>/planning/flow-plan.json` 与各 step review

默认不要把这些文件当成人类主编辑入口：

- `config/ingest-roots.json`
- `store/assets.json`
- `store/spans.json`
- `store/spans.meta.json`
- `.tmp/edit-flow/<editId>/timeline/current.json`（本机临时审计，不是正式同步产物）

## 当前阶段判断

当用户没有明确说 phase 时，按以下顺序判断：

- 没有 `store/project.json` / `store/manifest.json`：先做 `Project Init`
- 有项目骨架但没有 `store/assets.json`：先做 `Ingest`
- 有 `store/assets.json` 但缺少对应 `analysis/asset-reports/*.json`：先做 `Analyze`
- 如果已有完整 `analysis/asset-reports/*.json`，用户只改了 span 派生口径，优先去 `/chronology` 运行 `生成素材片段与模式`，不能默认重跑完整 Analyze
- 如果用户只改了 GPS / Pharos context / 空间优先级 / reverse-geocode 逻辑，先运行 `spatial-refresh` 修补 reports，再回 `/chronology` 重新生成 spans 和 chronology
- 有 `store/spans.json` 但缺 fresh `store/spans.meta.json`，或 `media/chronology.json` 缺失、仍是旧数组 v1、或不是 `status=confirmed` 的 Chronology V2：先去 `/chronology` 生成素材片段与模式、生成/审查/确认 chronology
- 有 confirmed chronology，但缺少 `edits/<editId>/config/edit-unit.json` 或其中没有 `editRuleCategory`：先进入 `/edit` 初始化 Edit Unit
- 有 Edit Unit 但缺少 confirmed `edits/<editId>/planning/flow-plan.json`：由 Codex Agent 生成/更新 Flow Plan；如果后续 capability 声明需要 fresh spans，再由该 step 精确阻塞
- 有 confirmed Flow Plan 但缺少当前 `plannerPolicyVersion=codex-agent-v1` 或 `materialIdPolicyVersion=human-source-v1`：视为旧计划，由 Codex Agent 重新生成
- 有 confirmed Flow Plan：继续由 Codex Agent 处理下一个未完成或待审 capability step
- Flow Plan 已完成且存在正式时间线 / NLE 输出产物：进入 `Export`、Resolve timeline 交互修改或迭代修改

如果用户是追加素材，不要回到全新项目口径；应按：

- 增量 `Ingest`
- 增量 `Analyze`
- 由 Codex Agent 重做或续跑受影响的 Flow Plan steps
- 重新 `Export`

## 必须阻塞并先说明的停点

- 在 `Analyze` 前，必须先说明 GPS 优先级：`embedded GPS > Pharos GPX > 普通 project GPX > project-derived-track > none`
- 在 `Analyze` 前，必须先确认 ML server 可用；不可静默退化成无 ML 分析
- 在 `Analyze`、`Style Analysis` 或任何可能使用 ML 的正式流程结束后，必须确认 Kairos 官方 ML service 已回收到 `stopped`
- 如果 ingest 因素材时间冲突被阻塞，必须先把用户引导到 `/ingest-gps` 的“素材时间校正”卡片；默认先填 `正确时间 / 时区`，只有自动补日期失败时才要求再补 `正确日期`
- “素材时间校正”是 capture-time 修正的唯一编辑入口；不要要求用户再去 `导入 / GPS Review` 填同一批字段
- 如果 root 声明了 `captureTimePolicy.mode=manual-required`，对应素材的时间阻塞必须要求用户显式填写 `正确日期 / 正确时间 / 时区`；不能用当前文件时间或建议日期自动代填后继续 Analyze
- 如果用户刚更新了项目内 `pharos/`，必须先引导到 `/ingest-gps` 运行 `刷新 GPS 缓存` 或 `运行 Ingest`，让 `analysis/pharos-context.json` 重新解析后再进入 Analyze
- 如果用户只是需要让旧 `asset-reports` 采用新的 GPS / Pharos 空间结果，应引导到 `/chronology` 的空间刷新入口；它不会补扫新素材或新 sidecar，完成后还需要重新生成 spans / chronology
- 在 `Edit Flow` 前，如果 chronology 不是已确认 Chronology V2，必须先把用户引导到 `/chronology`；fresh spans 只在 Flow Plan step 的 `inputRefs` 明确声明时阻塞，`trip.event_table` 这类事件组织能力不得因为 spans stale 而停下
- Edit Flow 的 `sharded-agent` step 必须按 confirmed `step.execution.shardPacking` 做连续天打包，并在 handoff 中写入 `codexSubagentProfile={reasoningEffort:"high", forkContext:false, speed:"standard"}`；不要按 route 做默认一级分片，也不要让正式 packet 携带完整 `spans.json` / `chronology.json`
- 在 `Edit Flow` 前，如果用户没有明确指定 workspace 剪辑规则分类，必须停下确认
- 如果剪辑规则要求使用风格层，但用户没有选择 `styleCategory`，或选择的是 legacy 非 layered-v1 档案，必须先引导用户在 `/edit` 选择 / 重跑 `/style`，不能继续执行 Edit Flow step
- 不要把 Console 的 `script` job 或 `/script` 描述成当前正式入口；Chronology 后的正式入口是 `/edit`
- 如果 Flow Plan step 有 human gate，必须通过产物审查确认后才能继续依赖 step；不要新增 `/edit` 确认按钮作为执行器
- 在 `Export` 前，必须先确认目标 NLE 和最终输出目录；目标不明确时不能直接写出草稿

## 对用户的推荐表达方式

- 如果用户问“怎么开始”，优先给出当前项目的下一步，而不是整本手册
- 如果用户问“整个流程是什么”，先给 5-7 步的高层顺序，再补当前项目卡在哪一步
- 如果用户问“继续做”，优先指出当前已完成产物和最近可继续的 phase
- 如果用户只要求某一阶段，不要强行展开全流程说明；只在必要时补充它的前置条件和后续交接

## 禁止行为

- 不要把 `kairos-workflow` skill 当成单个 phase 的替代实现
- 不要跳过 `Project Init` 就直接假设项目配置已正确
- 不要让用户直接维护 `assets.json` / `spans.json` / `spans.meta.json` 这类派生产物
- 不要在 Flow Plan 未确认的情况下执行 edit capability step
- 不要在导出目标未确认时直接进入 Export
