---
description: 官方运行时、live job 语义、durable progress cache 和 ML 生命周期的统一真值
alwaysApply: true
---

# Runtime Service Truth

只要任务涉及 Kairos 的正式运行入口、Supervisor、Console 监控、ML service、Analyze、Style Analysis、Edit Flow 或 job 生命周期，就必须遵守这份规则。

## Official Runtime Contract

- 当前唯一正式本地运行入口是 `Supervisor + React console (apps/kairos-console/)`
- 正式监控路由是：
  - `http://127.0.0.1:8940/analyze`
  - `http://127.0.0.1:8940/chronology`
  - `http://127.0.0.1:8940/edit`
  - `http://127.0.0.1:8940/style`
- 正式 Ingest / GPS 刷新入口是 `http://127.0.0.1:8940/ingest-gps`；它应通过 Supervisor `ingest` / `gps-refresh` job 显式运行，保存配置本身不代表刷新已经执行
- `ingest` 的正式刷新范围包括 `store/assets.json`、`gps/derived.json` 与 `analysis/pharos-context.json`；对成功扫描到的 root，磁盘上已删除的旧资产应从资产索引中移除
- `gps-refresh` 的正式刷新范围包括项目级 GPX merged cache、`gps/derived.json` 与 `analysis/pharos-context.json`；它不扫描素材目录
- `analysis/pharos-context.json` 是 Ingest/GPS 刷新的 Pharos context cache，必须按项目内 `pharos/` 输入 fingerprint 和 parser version 自动失效；不要把 stale empty context 当成当前 Pharos 真相。若只是 parser 升级补齐 Pharos shot 执行语义，允许直接重跑 `chronology-build`，不要因此要求 `span-rebuild`
- `/analyze` 不提供 span / chronology 生成入口；Analyze 只生成或更新 `analysis/asset-reports/*.json` 与细扫结果
- `/chronology` 是 span-builder + Chronology V2 审查入口：先显式运行 `span-rebuild` 生成候选 `store/spans.json` / `store/spans.meta.json`，其中 provisional `materialPatterns[]` 由本地 qwen 文本 LM 根据 span 级文本事实按素材时间顺序 8 个 candidate span 一批生成；Analyze 必须为 keep 的非音频素材产出可用视觉描述，`span-rebuild` 发现缺失 report 或缺失 `visualObservation` 必须失败并提示回到 Analyze，不得猜测补写；新 fine-scan report 必须保留来源 `sourceInterestingWindowIds / sourceWindowReason`，speech/mixed 窗口必须自带裁剪 transcript truth；历史 fine-scan window 只有在缺 `semanticKind`、自身或来源证据为 `speech-window` 且与 transcriptSegments 重叠时才保守恢复 speech truth，明确 `semanticKind=visual` 不因 transcript overlap 自动转 speech；同一素材内 speech/mixed 与 visual span 可以重叠共存，speech 只承载裁剪口播 truth，visual 必须保留完整视觉窗口和 `visualObservation`，不得因为 speech 重叠被切断、缩短或继承 transcript；相邻 speech/mixed 候选按同素材、同口播通道、间隔 `<=3000ms`、合并后 `<=45s` 且可见中文 transcript `<=160` 字合并；行车 visual fallback/fine-scan 窗口按 passage 降碎片，默认同素材相邻 gap `<=60s`、单段 `<=90s`，speech/mixed 行车 span 仍独立并可与 visual passage 重叠；LM prompt 只请求按顺序返回 `materialPatterns` rows，Supervisor 不再裁决 speech keep/drop、visual-only 转换或口播时间收缩；第 1 项只描述素材自身可观察的拍摄视角/构图形态，不得重复 photo/video 等载体类型或写“建场/记录/成果”等后续剪辑用途；任一必需槽缺失或冲突都进入 failed span 列表并在主 chunk 后单条补处理，补处理仍不合格则阻塞本次 rebuild，不做启发式改写或兼容映射；如果存在 speech/mixed candidate span，meta 必须标记 `pending-speech-review` 并写 `.tmp/chronology/speech-window-agent-handoff.md`，由 Codex/Agent SubAgents 完成最终 speech-window 裁切、drop/visual-only 和 slot 4 对齐后写回 `status=fresh`；speech-window review 的每个 SubAgent shard 最多约 1500 个 candidates，并尽量保持同一 asset/day 不跨 shard，且不得缩短、删除或切碎重叠 visual span；再运行 `chronology-build` 生成 `media/chronology.json`；页面必须显示 `.tmp/chronology/progress.json` 的 live job 进度摘要、ETA、失败列表与重试统计，并可参考 `.tmp/chronology/span-rebuild.partial.json` 查看已完成 checkpoint 和 failed spans；正式 `store/spans.json` 只在全量收口后写入
- `chronology-build` 必须写自己的 live progress，不得沿用旧 `span-rebuild` cache；进度应覆盖输入加载、Pharos/GPS 加载、asset 时间索引、inputs hash、span 时空归属解析、row 排序、event/route 聚合、GPS reverse-geocode 地名解析、gap 生成、review 状态合并和写入结果，并在长循环中按批次更新 row/event/route/Pharos/geocode 计数。
- 项目级 `chronology-build` 是 GPS reverse-geocode 强依赖：UI、Supervisor、CLI 或库函数只要要写 `media/chronology.json`，都必须创建或注入可用 service；显式 `null` service、无 provider 且 cache miss、或任一 route/event GPS anchor 反查不到地名时必须失败并保留既有 chronology，不能用 `placeHints`、`materialPatterns`、manual-itinerary prose、Pharos continuous prose 或英文通用地点文本兜底写入。
- `chronology-build` 的正式归属顺序是：先用 Pharos 单点事件真实 actual window，span 与 `expected / unexpected` 且非 `continuous` 的 window 存在有意义重叠时可直接归入该普通 `event`；多个 Pharos 单点事件时间窗重叠时，只按 `record.json.actual_captures[]` 等显式拍摄类型/设备字段调整归属优先级，仍同分时优先更窄的 actual window，不从描述、地点或 note 文本推断航拍等语义；Pharos 单点事件是 route 硬边界，Pharos `continuous` 只提供 route 的时间 / summary 上下文，不把多个事件间 route 强行合并，也不得把 `深圳 → 南宁 全程` 这类 route prose 写入 `event.location / route.from / route.to`。Chronology 聚合的 GPS 优先级为 Pharos trip GPX / 项目 `gps/merged.json` / `gps/derived.json` / report 中 `pharos|gpx|derived-track` / embedded GPS 兜底；route 用自身 `startAt/endAt` 取端点 GPS 并通过 `gps/reverse-geocode-cache.json` cache-first 反查，普通非 Pharos event 用 midpoint GPS 反查；普通 event 只按 chronology 相邻顺序的时间连续性与 GPS 轨迹连续性聚合，静态近距离阈值为相邻代表点 `<=400m`，移动中非 route 观察可在相邻间隔 `<=5min` 且两点间速度连续合理时合并；`route` 只由结构化 `drive` 素材和 route cluster 的短间隔伴随片段产生；普通非 Pharos 照片不参与一阶 event/route 切分，不能作为 event 打断 route；照片先按时间范围优先挂到 route，剩余照片再按时间最近挂到普通 event；事件/路线 `spanIds` 按粗剪友好顺序写入，非照片 span 保持事件内顺序，照片统一追加到事件末尾，`startAt/endAt` 仍由全部 rows 的真实时间范围决定；`materialPatterns / visualObservation / transcript`、反查地名和标题不得参与 event/route 聚合判定
- Pharos 单点事件是人工行程事实，`chronology-build` 生成后默认 `reviewStatus=confirmed`；无素材命中的 Pharos `gap` 仍是待审缺口，默认 `pending`
- `spatial-refresh` 是 no-ML deterministic job：只刷新已有 `analysis/asset-reports/*.json` 的 GPS / Pharos / reverse-geocode 空间字段，不扫描素材、不抽帧、不转写、不生成缺失 report，也不写 `store/spans.json` 或 `media/chronology.json`
- `/chronology` 是 Analyze 与 Edit Flow 之间的正式 Chronology V2 审查入口；Edit Flow capability 只能消费 `schemaVersion=2.0` 且 `status=confirmed` 的 `media/chronology.json`
- `/chronology` 的列表时间与日期筛选必须按项目 Pharos trip `timezone` 显示；不得把 ISO `Z` 字符串直接截断当作本地时间。缺少有效 trip timezone 时才回退浏览器时区，磁盘 chronology 时间字段仍保持 ISO 真值。
- Analyze 的事实真相在 `analysis/asset-reports/*.json`；keep 的非音频 report 必须携带可用视觉描述，fine-scan recognized window 必须携带 `visualObservation`；`store/spans.json` 是可由 assets + asset reports + local qwen text LM 重建的素材片段索引，`media/chronology.json` 是可由 assets + fresh spans + root time + Pharos context 重建的项目级编年史
- `interestingWindows[]` 是细扫前计划，`fineScanWindows[]` 是细扫后的 recognized / dropped 窗口结果；新窗口必须保留稳定来源血统，已有完整 `fineScanWindows` 时，重建 spans 不应启动 VLM/ASR 或重新视觉分析
- Analyze 运行过程中不得在 coarse/direct、fine-scan 或 finalize 阶段直接 append / rebuild span；`store/spans.json` 只由显式 `span-rebuild` job 从全项目 reports 重建，旧 spans 不参与 fine-scan 待办判断或语义恢复
- `spatial-refresh` 只消费已经写入 `store/assets.json` 的 embedded GPS 绑定；新增或修改 `.SRT`、FlightRecord、素材 root、root 时钟偏移或 capture-time override 后，仍必须先运行 `ingest`
- `scripts/kairos-supervisor.* start` 只启动 `Supervisor + React console`
- `scripts/kairos-supervisor.* start` 不会自动启动 ML
- `scripts/kairos-supervisor.* start` 不会自动恢复旧 job
- 只要改动影响正式本地运行入口、Supervisor API、`/analyze`、`/chronology`、`/edit`、`/style`、`/color` 或 `apps/kairos-console/` 前端，验证就必须同时包含：
  - 根仓 `pnpm build`
  - `npm --prefix apps/kairos-console run build`
- 不要把根仓 `pnpm build` 误当成已经覆盖 React console 产物；前端 bundle 必须单独构建后，才能宣称 UI 变更已完成验证

## Live Job Truth

- `progress.json` 只是 durable progress cache，不等于 live job
- `projects/<projectId>/.tmp/media-analyze/progress.json` 不是 Analyze 或 spatial-refresh 仍在运行的证据
- Analyze 续跑不能把 `progress.json.step` 当恢复指针；启动后必须从真实产物推导待办。若 coarse report 已全部存在且仅剩 fine-scan，首个 live progress 应进入 `fine-scan-prefetch`，不能误写回 `prepare`。
- `projects/<projectId>/analysis/pharos-context.json` 也不是 Pharos 输入没有变化的证据；判断是否最新必须看 cache 内的 source fingerprint
- `<workspaceRoot>/.tmp/style-analysis/<category>/progress.json` 也不是 style-analysis 仍在运行的证据
- 判断“流程是否真的还在跑”，必须至少同时核对：
  - Supervisor 中是否存在匹配的 active job
  - `progress.json.updatedAt` 是否持续推进
  - GPU / ML / worker 活动是否和当前 phase 一致；`spatial-refresh / chronology-build` 是 no-ML job，不应期待 GPU / Kairos ML service 活动；`span-rebuild` 会启动本地 ML 服务调用 qwen 文本 LM，但不重跑 VLM / ASR

## Capability Truth

- Console 的“可启动 / 运行中 / 完成”只能来自真实 capability 和 live job
- 任何 jobType 只要 runner 没接好，就不能在 capability 或 UI 上继续表现成正式可运行
- stale progress 只能显示 cached / idle，不得伪装成 running
- `timeline.generate` 处理照片时默认静帧时长为 `1000ms`；只有剪辑规则 / confirmed Flow Plan 或运行时 `timelineStillDurationMs` 明确覆盖时才改变。Resolve host 必须回读校验实际照片时长，不能把旧 `5s` 或 Resolve 偏好当作默认合同。Resolve rough cut 还必须写入并回读校验批处理 clip color：普通有声视频/linked audio 为 `Orange`，照片 video item 为 `Blue`，延时 video item 为 `Purple`；有声延时的 linked audio item 仍用 `Orange`。

## ML Lifecycle Truth

- Kairos 官方管理且会使用 / 管理 ML 的顶层流程结束态必须是 `ML stopped`
- 这里的 ML-backed 顶层流程至少包括：`analyze`、`style-analysis`，以及未来明确接入 ML lifecycle 的正式流程
- `spatial-refresh / chronology-build` 是 no-ML deterministic job；`span-rebuild` 是本地 qwen text-LM job，会按 Supervisor ML lifecycle 启动并在结束后停止 Kairos 管理的 ML 服务
- 结束路径包括：正常完成、失败退出、用户 stop、restart 前置 stop、以及中断收尾
- 低层调试命令可以单独启动 ML，但正式流程收尾时仍必须把 Kairos 官方 ML service 对账回 `stopped`
