---
description: 只有 confirmed Edit Flow 明确要求 script.generate 时才执行 kairos-script skill
alwaysApply: true
---

# Script Skill Enforcement

当任务涉及生成、重写、续写、审查或更新 `edits/<editId>/script/current.json`、旁白、beat 文案或 `script.generate` capability 产物时：

- 必须先确认当前 edit unit 的 `edits/<editId>/planning/flow-plan.json` 已 confirmed，且其中明确包含 `script.generate` step；没有该 step 时，不得把脚本生成当成默认流程。
- 新剪辑工作默认从 `/edit` 和 `kairos-edit-flow` 开始；不要把 `/script`、`script` job、`script/current.json` 解释成正式必经入口。
- 必须先读取并遵循 `kairos-script` skill，不能跳过 skill 直接按临时理解执行。
- 必须遵守 `kairos-script` 里显式标注的 `[main agent]` / `[subagent:*]` 边界；主代理负责 orchestration，subagent 负责各自阶段产物，不能互相偷换。
- 如果 `kairos-script` 要求 clean-context subagent / reviewer 链，而当前宿主策略或用户授权不允许启动这条链，必须先停下说明原因；不能静默折叠成单代理本地起稿。
- 如果当前宿主本身是 agent-base 环境，缺 formal stage runner 时必须先说明“宿主 packet runner 未接通 / 未配置”，不要把问题改写成“请用户提供 LLM key”。
- script stage packet 是 formal subagent 的唯一上下文；不要再在 packet 外重复附加 `previousDraft`、`revisionBrief` 或主线程历史。
- 当 `kairos-script` 明确写明“剪辑规则 / 风格不驱动默认时长”时，不得从 edit rule、style 平均章节时长、模板习惯或材料容量自动补总时长、段落预算或 `targetDurationMs`。
- 当剪辑规则要求使用风格层时，只能读取 confirmed `edits/<editId>/planning/flow-plan.json.styleUsage` 授权的 `literary / artistic / editingTechnical` 层；不得从 edit-rule markdown 或 legacy style profile 长文里关键词解析使用权。
- 如果 `styleUsage` 需要任何风格层但 `styleCategory` 缺失，或 profile 不是 `styleProfileVersion=layered-v1`，必须阻塞并提示重跑 `/style` 或选择可用分层档案。
- `material.recall` 的正式结构化产物只有 `edits/<editId>/script/material-slots.json`；不要生成、消费或要求 `segment-plan.json` 作为 Edit Flow 正式输出。
- `material-slots.json` 是正式 `material-slots` truth；每个 `chosenSpanId` 必须有 numeric `treatments[spanId]={audio,speed}`，`audio` 是 dB（默认 `0`，静音 `-100`），`speed` 是倍速（默认 `1`）。非照片 span 如果有 transcript、transcriptSegments 或 `semanticKind=speech/mixed`，不得写 `audio<=-100`。不要再让 `route-slot-planner` 改写 `chosenSpanIds`，也不要让 `mixed / audio:* / speed:*` 这类文本建议进入正式字段。
- `beat-writer` 只能改写表达层字段（如 `text / utterances / notes / muteSource / preserveNatSound`）；不要让它增删或改写 `audioSelections / visualSelections / linkedSpanIds`。
- 在判断“剪辑规则缺失”之前，必须先检查当前 edit unit 是否已有 confirmed Flow Plan；Flow Plan 是正式输入，`script-brief.md` 不是新流程的 gate truth。
- 只有在 skill 要求的关键输入经核对后仍缺失、冲突或无法映射到实际文件时，才能按缺失输入规则停下并询问用户。

## 禁止行为

- 不要在没读 `kairos-script` skill 的情况下直接生成脚本
- 不要在 Flow Plan 没有 `script.generate` step 时生成脚本
- 不要忽略当前项目 / edit unit 已经写明的剪辑规则，又重复向用户追问同一信息
- 不要只因为当前项目目录下没有规则文件，就跳过对 workspace 级 `config/edit-rules/*.md`、已确认 Flow Plan 与已存在 brief 的核对
- 不要在未获授权或宿主不支持时，把 subagent / reviewer 链静默折叠成单代理起稿
- 不要在 agent-host 场景把“formal subagent runner 缺失”错误地转嫁成“用户没给 API key”
- 不要让 `[main agent]` 事后“补锅”式改写 `script/current.json`，包括把 `{ "segments": [...] }` 再手工 normalize 成正式稿
- 不要为 `script-current` 额外预跑一轮 full-script writer；正式 `beat-writer` 每次 attempt 只应写一次
- 不要在 writer / reviewer 已失败后，还让 `script/agent-pipeline.json` 继续停留在旧阶段的 `pending`
- 不要把剪辑规则或风格参考当作默认时长模板，也不要用 style 平均时长、模板习惯或材料容量自动填写总时长、段落预算或 `targetDurationMs`
- 不要让 `route-slot-planner` 静默删掉 deterministic base `chosenSpanIds`；对 `material-slots` 的 silent span drops / recall regression 必须显式拦下
- 不要把静音/加速建议藏在 query、targetBundles 或自然语言里；正式读取只看 numeric `treatments`
- 不要让 `beat-writer` 借改文案之名改写 locked recall facts（`audioSelections / visualSelections / linkedSpanIds`）
